source 'https://rubygems.org'

gem 'asciidoctor-revealjs',	github: 'asciidoctor/asciidoctor-reveal.js', branch: 'revealjs-backend'
# NOTE For some reason, the json gem is required when using the system Ruby
gem 'json'
